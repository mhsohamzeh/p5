function setup() {
  // put setup code here
  createCanvas (1250,900)
  background(0);
}

function draw() {
var faceWidth=750;
var faceHeight=faceWidth;

//draw face outline
ellipse(width/2,height/2,faceWidth,faceHeight);

}
